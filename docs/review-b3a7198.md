# b3a7198 业务语义与页面回归复查

复查范围：该提交中的页面、请求 DTO、模型保存与部署、框架配置、Kubernetes 资源构建，以及相关迁移和配置。保留接口契约、访问控制、生命周期、分页和资源释放修复，不整体回退提交；保留后续 bde0af2 已恢复的表单样式。

## 已纠正

| 问题 | 修复 |
| --- | --- |
| 模型列表用 `code / PVC` 替换 `version / 版本`，同时删除默认镜像和启动命令 | 恢复三个业务列，按各自字段展示并转义输出，PVC 不充当版本 |
| 模型表单删除版本和描述，用 `custom / 备注` 承接信息 | 恢复独立版本和描述字段，保留已有备注，不推测或迁移备注含义 |
| 以原数据库没有字段为由删除页面信息 | 新增 V3 迁移及 DTO、保存、读取链路，版本、描述、默认镜像和命令均可保存；不改生成的 Base 模型和已执行的 V1/V2 |
| 模型地址被改成只读 `/model`，部署也忽略已配置路径 | 恢复可编辑模型地址，部署校验绝对路径并使框架命令与卷挂载使用同一路径；命令中的路径做 shell 引号处理 |
| 登记模型强制先配置 PVC | PVC 作为独立可选配置保留，部署时仍严格校验，缺少 PVC 不创建服务记录 |
| 服务列表以状态说明替换执行命令 | 恢复执行命令列，状态说明作为独立列保留 |
| 模型运行入口由跳转服务页改为留在模型页的弹窗 | 恢复跳转服务页，自动打开携带所选模型的部署表单，保存后可直接查看服务状态 |
| Java 资源构建丢失旧模板中的推理节点污点容忍 | 补回 `bussanq.com/run / Exists / NoSchedule`，保留命名空间、探针和归属校验 |

默认镜像和命令只在部署框架与模型配置一致时使用；切换框架采用所选框架的默认配置，避免把 vLLM 命令带入 SGLang。旧记录新增字段为空，不从 PVC、备注或其他字段推导版本等元数据。

## 保留的必要修复

- 模型字段 `name / type / id`、接口地址和 JSON 请求参数修正，动态模型选择及副本数传递。
- 登录、CSRF、输出转义、只读角色，以及生命周期状态和删除清理。
- SGLang 配置修复与未知推理框架校验。原 Ollama/TensorFlow/PyTorch 选项没有相应的可用部署实现，不恢复为可部署选项。
- 服务编辑中的推理类型保留当前实现；原 TFJob/PyTorchJob 属于训练任务，不作为推理服务类型恢复。
- 原“代码 git”输入绑定整型 `codeId`，且没有解析为代码仓库/同步配置的后端链路，不恢复该失效输入。
- 服务详情保留当前可用查询；原 `tensorBoard` 请求在服务控制器中没有实现，不恢复失效接口。
- 服务名称不可修改的约束保留，避免运行中资源与网关标识发生不一致；本次未引入重命名流程。

## 验证

- 只运行受影响模块：`FrameWorkServiceTest`、`TaskValidationTest`、`ServingResourcesTest`、`DatabaseLifecycleTest`、`ModelMetadataTest`，共 8 项测试通过。
- 独立临时 MySQL 库验证 V1→V3、新增元数据读写和部署参数；另验证 V2→V3 SQL 升级保留原 PVC、路径和备注，新增字段为空。
- 独立本地应用验证模型增改查删及 5 个页面的 HTTP 渲染；检查页面 JavaScript 语法、恢复的表格列，以及携带模型 ID 跳转服务页并打开表单的行为。
- 未运行全量测试，未连接真实 GPU/网关。浏览器连接不可用，未完成视觉检查或截图。
- 测试使用临时数据库，验证后清理。随后按要求备份本机业务库并重启 8081 服务，V3 迁移成功，数据库及 Kubernetes 健康检查正常，模型页面和列表接口验证通过。
